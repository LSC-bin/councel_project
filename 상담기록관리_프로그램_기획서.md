# 학생 상담·생활지도 기록 관리 프로그램 개발 기획서

> 2026년 부산광역시교육청 업무자동화 프로그램 개발대회 출품용 (교육 분야)
> 개발 도구: Electron + React, VS Code / Claude Code / 1인 개발
> 접수 마감: 2026.10.1.(목) 17:00

---

## 1. 프로젝트 개요

### 문제 정의
담임교사·상담교사·Wee클래스 담당교사 등 학생을 상담·생활지도하는 모든 교사는 상담 기록을 종이 메모, 개인 워드/한글 파일, 메신저 등에 산발적으로 남긴다. 이 때문에
- 학기말 상담활동 실적 보고서를 쓸 때 기록을 일일이 찾아 모아야 하고
- 특정 학생의 상담 이력을 한눈에 파악하기 어려우며
- 담임 교체 시 인수인계가 누락되기 쉽고
- 위기 징후(짧은 기간 내 상담 급증 등)를 놓치기 쉽다.

### 목표 사용자
담임교사에 한정하지 않고 **모든 교과 교사, 상담교사, Wee클래스 담당, 학년부장** 등 학생 상담·생활지도 기록이 필요한 모든 교직원.

### 핵심 원칙 (대회 규정 준수)
- **완전 로컬 동작**: 외부 API·AI·클라우드 서버로 데이터 전송 없음
- **특정 개인 계정 비의존**: Google/Notion 로그인 연동 없음
- **무료 SW만 사용**
- **단일 실행파일 배포**: electron-builder portable 타깃
- **개인정보 보호**: 로컬 암호화 저장 + 앱 잠금

---

## 2. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | Electron | 데스크탑 앱, 크로스플랫폼 |
| UI | React + TypeScript | 컴포넌트 기반 |
| DB | SQLite (`better-sqlite3`) | 메인 프로세스에서 동기 접근 |
| 암호화 | Node 내장 `crypto` (AES-256) | 백업 파일 암호화 |
| 차트 | `chart.js` + `react-chartjs-2` | 통계 화면 |
| 엑셀 | `xlsx`(SheetJS), `exceljs`(서식 있는 출력) | 명부 업로드 / 보고서 출력 |
| 라우팅 | `react-router-dom` | 화면(탭) 전환 |
| 패키징 | `electron-builder` (portable) | 설치 없는 단일 exe |

---

## 3. 데이터베이스 스키마

```sql
CREATE TABLE students (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    student_no TEXT,
    class_name TEXT,
    pinned BOOLEAN DEFAULT 0,
    active BOOLEAN DEFAULT 1,
    archived_year TEXT               -- 기능⑪ 학년도 아카이브용, NULL이면 현재 학년도
);

CREATE TABLE consult_types (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    color TEXT
);

CREATE TABLE quick_templates (
    id INTEGER PRIMARY KEY,
    type_id INTEGER REFERENCES consult_types(id),
    text TEXT
);

CREATE TABLE consult_records (
    id INTEGER PRIMARY KEY,
    student_id INTEGER REFERENCES students(id),
    type_id INTEGER REFERENCES consult_types(id),
    record_date DATE NOT NULL,
    content TEXT,
    state_score INTEGER,             -- 1~5, NULL 허용
    follow_up_needed BOOLEAN DEFAULT 0,
    follow_up_done BOOLEAN DEFAULT 0,
    next_appointment DATE,
    referred_to TEXT,                -- 콤마 구분: Wee,학폭담당,보건교사,학부모,기타
    reflected_in_nice BOOLEAN DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE settings (
    key TEXT PRIMARY KEY,
    value TEXT                        -- app_password_hash, nice_deadline_date 등
);
```

---

## 4. 화면 구성 상세

### 4-1. 대시보드 (`Dashboard.tsx`)
- **상단 요약 카드 3개**: 이번 달 상담 건수 / 후속조치 대기 건수 / 등록 학생 수
  - 쿼리: `SELECT COUNT(*) FROM consult_records WHERE record_date >= 이번달1일`
  - 후속조치 대기: `follow_up_needed=1 AND follow_up_done=0`
- **위기 징후 알림 배너** (기능①, 있을 때만 노출): 빨간 배경 배너로 "OOO 학생 최근 14일간 3회 이상 상담" 형태 표시, 클릭 시 해당 학생 조회 화면으로 이동
- **최근 상담 기록 리스트**: 최신순 10건, 학생명 + 유형 배지(색상) + 날짜
- **즐겨찾기 학생 영역** (기능⑩): 핀 고정된 학생 아바타를 상단에 가로로 나열, 클릭 시 해당 학생 조회 화면 이동

### 4-2. 기록 입력 (`RecordInput.tsx`)
- 학생 검색 인풋(자동완성 드롭다운, `students` 테이블 name/student_no로 필터)
- 날짜 선택(기본값 오늘)
- 상담 유형 선택(드롭다운, `consult_types` 목록 + "유형 관리" 버튼으로 추가/수정 가능)
- **빠른 입력 템플릿 버튼 그룹** (기능③): 선택된 유형에 해당하는 `quick_templates`를 버튼으로 나열, 클릭 시 내용 textarea에 텍스트 삽입(커서 위치에 append)
- 내용 textarea
- 상태 점수 선택 (기능⑥): 1~5 라디오 또는 슬라이더, "직전 상담 대비" 참고용으로 직전 기록의 점수를 옆에 회색으로 표시
- 후속조치 필요 체크박스 → 체크 시 다음 상담 예정일 date picker 노출 (기능⑧)
- 유관기관 연계 체크박스 그룹(Wee/학폭담당/보건교사/학부모/기타) (기능⑦)
- 생기부 반영 여부 체크박스 (기능④)
- 저장 버튼 → `records:add` IPC 호출 → 성공 시 대시보드로 이동 또는 입력 폼 초기화

### 4-3. 조회·검색 (`SearchView.tsx`)
- 상단 필터바: 학생명 검색, 기간(시작~종료), 유형 다중 선택
- 결과 테이블: 날짜/학생명/유형배지/내용 요약(1줄)/후속조치 상태 아이콘
- 행 클릭 → 상세 패널(우측 슬라이드 패널 또는 모달): 전체 내용, 수정/삭제 버튼
- **학생 선택 시 "상담 준비 브리핑 카드"** (기능⑫) 표시: 직전 상담 요약, 상태 점수 추이 미니 라인차트, 미해결 후속조치 하이라이트
- **학생별 상태 점수 시계열 그래프** (기능⑥): `react-chartjs-2` line chart, x축 날짜, y축 1~5

### 4-4. 통계 (`Statistics.tsx`)
- 월별 상담 건수 추이: bar chart
- 유형별 분포: doughnut chart
- 학생별 상담 횟수 랭킹: 테이블, 상위 10명
- **"익명화 통계 내보내기" 버튼** (기능②): 클릭 시 학생명을 A학생/B학생... 순으로 치환한 엑셀 파일 생성 (매핑은 파일 내에서만 유효, 원본 DB는 변경 안 함)

### 4-5. 보고서·백업 (`ReportExport.tsx`)
- 기간 선택 → "실적 보고서 생성" 버튼 → `exceljs`로 서식 있는 엑셀 파일 생성 (건수, 유형별 통계, 상세 목록 포함)
- **인수인계 요약카드 출력** (기능⑤): 학생 선택 → 해당 학생의 전체 상담 이력을 1페이지 요약 PDF로 출력 (Electron `printToPDF` 활용)
- **백업/복원** (기능⑨): "백업 만들기" → DB 파일을 비밀번호 기반 AES-256으로 암호화한 `.backup` 파일 생성 → 파일 저장 다이얼로그. "복원하기" → 파일 선택 + 비밀번호 입력 → 복호화 후 기존 DB 대체(백업 확인 경고 후 진행)
- **표준 인쇄 서식 출력** (기능⑭): 개별 상담 기록을 A4 1장 인쇄용 레이아웃으로 출력 (Electron `webContents.print()`)

### 4-6. 설정 (신규 — 아래 6장 참고)
- 상담 유형 관리(추가/삭제/색상 변경)
- 빠른 입력 템플릿 관리
- 앱 비밀번호 변경
- 생기부 마감일 설정 (기능④)
- **학년도 전환** (기능⑪): "새 학년도 시작" 버튼 → 현재 활성 학생들을 `archived_year`에 지난 학년도 값으로 기록 → 새 명부 업로드 화면으로 이동. 아카이브된 학년도는 조회 화면에서 "지난 학년도 보기" 토글로 확인 가능

---

## 5. 기능 상세 명세 (구현 지시사항 포함)

### 기능① 위기 징후 조기 감지 알림
- **목적**: 담임이 놓치기 쉬운 상담 급증 패턴을 시스템이 먼저 알려줌
- **로직**: 앱 시작 시 및 기록 저장 시마다, 각 학생에 대해 `record_date >= 오늘-14일` 조건의 레코드 수를 계산. 3건 이상이면 경고 대상으로 표시
- **구현**: `electron/db/database.ts`에 `getCrisisAlerts(): {studentId, name, count}[]` 함수 추가. 대시보드 마운트 시 IPC로 호출해 배너 렌더링
- **기준값(3건/14일)은 설정 화면에서 조정 가능**하도록 `settings` 테이블에 저장(스트레치)

### 기능② 통계 익명화 내보내기
- **목적**: 학년부장 회의·학교 보고용 자료를 개인정보 없이 공유
- **로직**: 통계 화면에서 조회된 학생 집합을 상담 횟수 내림차순 정렬 후 "A학생", "B학생"... 알파벳(26명 초과 시 A1,A2...) 매핑 테이블을 메모리에서만 생성 → `exceljs`로 유형별 건수, 추이 등을 매핑된 이름으로 시트 작성
- **구현**: `src/utils/anonymize.ts`에 매핑 함수, `ReportExport.tsx` 또는 `Statistics.tsx`에 내보내기 버튼

### 기능③ 빠른 입력 템플릿
- **목적**: 반복되는 상담 문구 타이핑 시간 절약
- **로직**: 유형 선택 시 해당 `type_id`의 `quick_templates` 목록을 버튼으로 렌더링, 클릭 시 textarea에 텍스트 삽입
- **구현**: 기본 템플릿 시드 데이터를 `schema.sql` 또는 초기 마이그레이션에 포함 (예: "교우관계 갈등 - 중재 완료", "학습 부진 상담 - 방과후 보충 안내"). 설정 화면에서 CRUD 가능

### 기능④ 생기부 마감 연동 알림
- **목적**: 학교생활기록부 작성 마감 전 미반영 상담 건 누락 방지
- **로직**: 설정에서 마감일 지정 → 마감일 D-7부터 대시보드에 "생기부 미반영 상담 N건" 알림 카드 노출, 클릭 시 `reflected_in_nice=0`인 레코드만 필터된 조회 화면으로 이동
- **구현**: `settings` 테이블 `nice_deadline_date` 값 활용, 대시보드에서 오늘 날짜와 비교하는 조건부 렌더링

### 기능⑤ 담임 인수인계 요약카드
- **목적**: 학기말/학년말 학생별 상담 이력을 후임 담임에게 전달
- **로직**: 학생 선택 → 해당 학생의 전체 레코드를 날짜순 정리, 상태 점수 추이 요약, 미해결 후속조치 강조 → 1페이지 레이아웃 컴포넌트로 렌더링 → PDF 출력
- **구현**: `src/components/HandoverSummary.tsx`(인쇄 전용 스타일) + Electron `printToPDF` 또는 `window.print()` 활용

### 기능⑥ 상담 전후 변화 추적
- **목적**: 상담이 실제로 효과가 있었는지 시각적으로 확인
- **로직**: 기록 입력 시 상태 점수(1~5) 저장 → 학생별 조회 화면에서 날짜 x 점수 line chart로 시계열 표시
- **구현**: `SearchView.tsx` 학생 상세 패널에 `react-chartjs-2` Line 컴포넌트 추가

### 기능⑦ 유관기관 연계 기록
- **목적**: 사안이 어떤 지원 체계로 연계됐는지 이력 관리
- **로직**: 다중 선택 체크박스(Wee/학폭담당/보건교사/학부모/기타) → 콤마 구분 문자열로 `referred_to`에 저장, 조회 화면에서 아이콘 배지로 표시
- **구현**: 기록 입력/조회 컴포넌트에 체크박스 그룹 및 배지 렌더링 함수 추가

### 기능⑧ 캘린더 뷰 + 다음 상담 리마인더
- **목적**: 상담 일정을 직관적으로 파악, 예정된 상담을 놓치지 않음
- **로직**: `next_appointment`가 설정된 레코드를 월간 캘린더에 점으로 표시. 오늘 이후 가장 가까운 예정 상담을 대시보드 상단에 "다음 상담: OOO, 9/5" 형태로 노출
- **구현**: 캘린더 UI는 간단한 자체 그리드 컴포넌트로 구현(외부 캘린더 라이브러리 없이 CSS Grid 42칸 방식이면 충분)

### 기능⑨ 암호화 백업/복원
- **목적**: PC 교체·다음 담임 인계 시 데이터 이동, 데이터 손실 방지
- **로직**: DB 파일(sqlite)을 통째로 읽어 Node `crypto`의 AES-256-GCM으로 비밀번호 기반 키 유도(`scrypt`) 후 암호화, `.backup` 확장자로 저장. 복원 시 반대 과정
- **구현**: `electron/db/backup.ts`에 `createBackup(password, savePath)` / `restoreBackup(password, filePath)` 구현, 메인 프로세스에서만 실행(보안)

### 기능⑩ 즐겨찾기(핀 고정)
- **목적**: 현재 집중 관리가 필요한 학생에게 빠르게 접근
- **로직**: `students.pinned` 토글, 대시보드 상단에 고정 학생 아바타 목록 표시
- **구현**: 학생 상세 패널에 핀 아이콘 버튼, 클릭 시 `students:togglePin` IPC 호출

### 기능⑪ 학년도 전환/아카이브 관리
- **목적**: 매년 바뀌는 담임 상황에서 과거 이력 보존 + 새 학년도 깔끔한 시작
- **로직**: "새 학년도 시작" 버튼 클릭 → 확인 모달 → 현재 `active=1`인 학생 전체의 `archived_year`에 이전 학년도 값 기록, `active=0` 처리 → 새 명부 업로드 화면으로 자동 이동. 조회 화면에는 "지난 학년도 보기" 토글 추가
- **구현**: 설정 화면에 버튼, `electron/db/database.ts`에 `archiveCurrentYear(yearLabel)` 함수

### 기능⑫ 상담 준비 브리핑 카드
- **목적**: 상담 직전 해당 학생의 맥락을 빠르게 파악
- **로직**: 학생 선택 시 직전 상담 1건 요약 + 상태 점수 추이 미니 그래프 + 미해결 후속조치 리스트를 카드 형태로 조합
- **구현**: `SearchView.tsx`에서 학생 선택 시 조합 쿼리 실행 후 `BriefingCard.tsx` 컴포넌트로 렌더링

### 기능⑬ 네이티브 데스크탑 알림
- **목적**: 후속조치 기한, 다음 상담 예정일을 놓치지 않도록 OS 레벨 알림
- **로직**: 앱 실행 시 및 매일 자정 기준으로 `follow_up_needed=1 AND follow_up_done=0` 이면서 등록일로부터 7일 경과, 또는 `next_appointment`가 오늘/내일인 레코드를 조회 → Electron `Notification` API로 트레이 알림 발송
- **구현**: `electron/main.ts`에서 `setInterval` 또는 앱 시작 시 1회 체크, `new Notification({title, body}).show()`

### 기능⑭ 표준 인쇄 서식 출력
- **목적**: 종이 결재·보관이 필요한 학교를 위한 인쇄 대응
- **로직**: 개별 상담 기록을 A4 1장 분량의 인쇄 전용 레이아웃(학교 로고 자리, 서명란 포함)으로 렌더링 후 인쇄
- **구현**: `src/components/PrintableRecord.tsx`(인쇄 전용 CSS `@media print`) + Electron `webContents.print()`

---

## 6. 개발 우선순위 (Phase)

### Phase 0 — MVP (필수, 약 2주)
학생 명부 엑셀 업로드 / 상담 기록 입력·조회·검색·수정·삭제 / 기본 통계(월별·유형별) / 앱 잠금 / 로컬 SQLite 저장

### Phase 1 — 차별화 기능 (핵심, 약 1주)
① 위기 감지, ② 익명화 내보내기, ③ 빠른 입력 템플릿, 엑셀 보고서 자동 생성

### Phase 2 — 완성도 강화 (남는 기간 순차 추가)
④ 생기부 알림, ⑤ 인수인계 요약카드, ⑥ 상태 변화 추적, ⑦ 유관기관 연계, ⑧ 캘린더/리마인더, ⑨ 백업/복원, ⑩ 즐겨찾기, ⑪ 학년도 전환, ⑫ 브리핑 카드, ⑬ 데스크탑 알림, ⑭ 인쇄 서식, 한글(hwpx) 출력(스트레치)

일정이 촉박하면 Phase 2는 번호 순서대로 자르면 됨. Phase 0+1만 완성해도 제출 가능한 수준.

---

## 7. 엑셀 명부 업로드 양식

| 번호 | 이름 | 학번(선택) |
|---|---|---|
| 1 | 홍길동 | 20601 |
| 2 | 김철수 | 20602 |

지정 양식 파일을 프로그램 내 "양식 다운로드" 버튼으로 제공, 업로드 시 헤더 검증.

---

## 8. 대회 제출 서류 관련 체크리스트

- [ ] [붙임 1] 프로그램 설명서: 사용 기술, 아키텍처, 실행 방법, 외부 라이브러리 목록과 라이선스 명시
- [ ] 현행 업무 방식 → 개선 방식 → 기대 효과 서술
- [ ] 시연용 데이터는 반드시 가상 학생명으로 생성, 실제 개인정보 사용 금지
- [ ] 개발자 PC 외 다른 PC에서 정상 구동 확인
- [ ] 백신 검사 완료 후 제출
- [ ] exe 파일은 zip으로 압축하여 제출

---

## 9. 향후 논의가 필요한 사항

- 상담 유형 기본값 최종 리스트 확정
- DB 자체 암호화(SQLCipher) 도입 여부
- 프로그램 이름 확정
